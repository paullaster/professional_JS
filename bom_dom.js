/**
 * BOM:
 * DOM
 */
//console.dir (navigator.geolocation);
//window.location.
//console.dir (location.ancestorOrigins)
history.go (-1);

//DOM
// while (document.readyState !== 'complete') {
//     console.dir ('Still loading...');
// };

//console.dir (document);

//Manipulating DOM

let body = document.body;
console.dir(body);